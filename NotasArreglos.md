

La sintaxis de un arreglo es tipo de dato, etiqueta y cuantos



la variable: "temp" es una direccion de memoria, la etiqueta s una direccion de memoria donde comienza el arreglo, el tipo de dato indica cada cuantos bytes bytes, esto se multplica por la cantifad de datos del arreglo, en memoria esto se ve como un salto de memoria segun la cantidad de bytes del tipo de dato 
